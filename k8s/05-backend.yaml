apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: iot-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: iot-backend:local      # buildée localement, voir README
          imagePullPolicy: Never        # ne va pas chercher sur un registre distant
          ports:
            - containerPort: 8000
          env:
            - name: POSTGRES_HOST
              value: postgres            # DNS interne = nom du Service
            - name: MQTT_HOST
              value: mosquitto
          envFrom:
            - secretRef:
                name: postgres-secret
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: iot-platform
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
